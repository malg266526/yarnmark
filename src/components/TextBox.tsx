import styled from 'styled-components';
import { Spacings } from '../styles/spacings';
import { Colors } from '../styles/theme';

export const TextBox = styled.div`
  padding: ${Spacings.lg};
  background: white;
  box-shadow: 3px 4px 4px 2px ${Colors.gold};
`;
