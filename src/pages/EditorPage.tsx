import React from "react";
import { Grid } from "../components/editor/Grid";

const EditorPage = () => {
    return (
        <div>
            <h1>Editor</h1>
            <Grid />
        </div>
    );
};

export default EditorPage;
